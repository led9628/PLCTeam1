class Main {
    public static void main(String[] args) {
        ArrayList<Token> tokenList = tokenize(args[0]);
    }

    public static ArrayList<Token> tokenize(String filename) {
        ArrayList<Token> tokens = new ArrayList<>();

        return tokens;
    }
}